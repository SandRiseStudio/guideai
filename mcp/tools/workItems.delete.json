{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "name": "workItems.delete",
  "description": "Delete a work item. If the item has children (e.g., an epic with stories), they will also be deleted (cascade).",
  "inputSchema": {
    "type": "object",
    "properties": {
      "item_id": {
        "type": "string",
        "description": "Work item ID to delete (required)"
      },
      "user_id": {
        "type": "string",
        "description": "ID of the user deleting the item"
      },
      "org_id": {
        "type": "string",
        "description": "Optional organization ID for multi-tenant context"
      }
    },
    "required": ["item_id"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "success": { "type": "boolean" },
      "deleted_id": { "type": "string" },
      "deleted_type": { "type": "string" },
      "cascade_deleted": {
        "type": "array",
        "items": { "type": "string" },
        "description": "IDs of child items that were also deleted"
      },
      "message": { "type": "string" },
      "error": { "type": "string" }
    }
  }
}
