{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "name": "messages.delete",
  "description": "Soft-delete a message. Only the original sender or a conversation admin can delete.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "message_id": {
        "type": "string",
        "description": "Message ID to delete (required)"
      },
      "user_id": {
        "type": "string",
        "description": "ID of the user deleting the message"
      },
      "org_id": {
        "type": "string",
        "description": "Optional organization ID for multi-tenant context"
      }
    },
    "required": ["message_id"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "success": { "type": "boolean" },
      "message": { "type": "string" }
    }
  }
}
