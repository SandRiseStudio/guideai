{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "name": "context.getContext",
  "description": "Get the current tenant context including active org, project, and authentication state. Returns identity info and available scopes. Useful for debugging and understanding the current session state.",
  "inputSchema": {
    "type": "object",
    "properties": {},
    "required": []
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "user_id": {
        "type": "string",
        "description": "Authenticated user ID (null if service principal auth)"
      },
      "service_principal_id": {
        "type": "string",
        "description": "Authenticated service principal ID (null if user auth)"
      },
      "org_id": {
        "type": "string",
        "description": "Current organization context (null if not set)"
      },
      "project_id": {
        "type": "string",
        "description": "Current project context (null if not set)"
      },
      "auth_method": {
        "type": "string",
        "enum": ["device_flow", "client_credentials", "none"],
        "description": "Authentication method used"
      },
      "roles": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Roles assigned to the identity"
      },
      "scopes": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Granted scopes for this session"
      },
      "is_authenticated": {
        "type": "boolean",
        "description": "Whether the session is authenticated"
      }
    }
  },
  "required_scopes": [],
  "category": "context",
  "surface_parity": ["CLI", "MCP", "API"]
}
