{
  "name": "compliance.fullValidation",
  "description": "Perform comprehensive compliance validation including policies, checklists, and audit trail generation in one call. This high-level tool combines compliance.validateByAction, compliance.validateChecklist, and compliance.auditTrail.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "action_id": {
        "type": "string",
        "description": "Action ID to validate (required)"
      },
      "policy_ids": {
        "type": "array",
        "items": {"type": "string"},
        "description": "Specific policy IDs to check. Empty = all applicable policies."
      },
      "generate_audit_trail": {
        "type": "boolean",
        "description": "Whether to generate audit trail entry for this validation",
        "default": true
      }
    },
    "required": ["action_id"]
  }
}
