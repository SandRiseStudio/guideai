{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "name": "context.setProject",
  "description": "Switch the current project context. All subsequent operations will be scoped to this project. For org-owned projects, the user must have access to the project's organization. For personal projects, the user must be the owner or collaborator.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "project_id": {
        "type": "string",
        "description": "Project ID to switch to. Must be accessible by the authenticated user/SP."
      }
    },
    "required": ["project_id"]
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "success": {
        "type": "boolean",
        "description": "Whether the context switch succeeded"
      },
      "project_id": {
        "type": "string",
        "description": "Active project ID"
      },
      "project_name": {
        "type": "string",
        "description": "Active project name"
      },
      "org_id": {
        "type": "string",
        "description": "Organization ID (if project belongs to an org)"
      },
      "error": {
        "type": "string",
        "description": "Error message if switch failed"
      },
      "error_code": {
        "type": "string",
        "description": "Error code for programmatic handling"
      }
    }
  },
  "required_scopes": ["context.switch"],
  "category": "context",
  "surface_parity": ["CLI", "MCP", "API"]
}
