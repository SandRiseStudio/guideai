{
  "name": "analytics.fullReport",
  "description": "Generate a comprehensive analytics report including costs, performance metrics, and ROI in one call. This high-level tool combines analytics.costByService, analytics.roiSummary, analytics.kpiSummary, and analytics.topExpensive.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "org_id": {
        "type": "string",
        "description": "Organization ID. Uses current org context if not provided."
      },
      "period_days": {
        "type": "integer",
        "description": "Number of days to analyze",
        "default": 30,
        "minimum": 1,
        "maximum": 365
      },
      "include_trends": {
        "type": "boolean",
        "description": "Include daily trend analysis",
        "default": true
      }
    },
    "required": []
  }
}
