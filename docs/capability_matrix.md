# GuideAI Capability Matrix

## Mission
Maintain a single source of truth for feature parity across all GuideAI surfaces (Web, REST API, CLI, MCP tools, and SDKs). This matrix ensures every shipped capability satisfies the MCP parity strategy in `MCP_SERVER_DESIGN.md`, the reproducibility promises in `REPRODUCIBILITY_STRATEGY.md`, and the release governance rules in `PRD_NEXT_STEPS.md`.

## How to use this document
1. **Before implementation** – Add or update the relevant row(s) to describe the new capability, noting target surfaces and required telemetry hooks.
2. **During review** – Link the corresponding parity tests, action records, and audit evidence to prove the capability works across every surface.
3. **Before merge/release** – Confirm the release checklist items reference this matrix and mark the capability status. Record action evidence via `guideai record-action` when updating the matrix.
4. **After release** – Update `PROGRESS_TRACKER.md`, `PRD_ALIGNMENT_LOG.md`, and `BUILD_TIMELINE.md` with the final status and cross-link to the matrix row.

## Capability overview
**Legend:** ✅ Complete | ⚠️ Adapter exists, manifests/endpoints pending | ⏳ Planned

**Summary (2025-10-23):**
- **50+ MCP tool manifests** shipped (`mcp/tools/*.json`)
- **40+ CLI commands** operational (`guideai --help`)
- **141 passing parity tests** across all 11 services (see `docs/PARITY_COVERAGE_MATRIX.md`)
- **50+ REST HTTP endpoints** operational across all services
- **🎉 Phase 1 Service Parity COMPLETE: 11/11 services with full CLI/REST/MCP coverage**

| Capability | Description | Web / UI | REST API | CLI | MCP Tool | SDK / Automation | Parity Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Onboarding quickstarts | Guided setups that take users from zero to first recorded action across surfaces with telemetry hooks. | Dashboard "Start Guided Run" flow | `POST /v1/runs`, `POST /v1/actions` | `guideai run`, `guideai record-action`, `guideai analytics metrics` | MCP onboarding playbook (planned) | `docs/ONBOARDING_QUICKSTARTS.md`, CLI + REST snippets | `docs/ONBOARDING_QUICKSTARTS.md`, `docs/analytics/onboarding_adoption_snapshot.md`, telemetry (`analytics.onboarding` via `web-console/dashboard/src/hooks/useOnboardingTelemetry.ts`) |
| Behavior handbook management | Create, approve, version, and retrieve handbook behaviors. | Behavior admin console | `POST /v1/behaviors`, `GET /v1/behaviors`, `GET /v1/behaviors/:id`, `PATCH /v1/behaviors/:id`, `DELETE /v1/behaviors/:id/draft`, `POST /v1/behaviors/:id/submit`, `POST /v1/behaviors/:id/approve`, `POST /v1/behaviors/:id/deprecate`, `POST /v1/behaviors/search` | `guideai behaviors create/list/search/get/update/submit/approve/deprecate/delete-draft` (9 subcommands) | `behaviors.create`, `behaviors.list`, `behaviors.search`, `behaviors.get`, `behaviors.update`, `behaviors.submit`, `behaviors.approve`, `behaviors.deprecate`, `behaviors.deleteDraft` (9 MCP tools in `mcp/tools/behaviors.*.json`) | Handbook SDK (`guideai.behaviors`) | ✅ **Full Cross-Surface Parity Complete** – SQLite runtime (`guideai/behavior_service.py` ~720 lines), REST/CLI/MCP adapters (`guideai/adapters.py`), CLI commands (`guideai/cli.py`), 9 MCP tool manifests (`mcp/tools/behaviors.list.json`, `behaviors.search.json`, `behaviors.get.json`, `behaviors.create.json`, `behaviors.update.json`, `behaviors.submit.json`, `behaviors.approve.json`, `behaviors.deprecate.json`, `behaviors.deleteDraft.json`), parity test suite (`tests/test_behavior_parity.py` - 25 passing tests covering create/list/search/lifecycle/update/error-handling/delete across all three surfaces), lifecycle documented in `docs/BEHAVIOR_VERSIONING.md`, audit obligations per `AUDIT_LOG_STORAGE.md` |
| Run orchestration | Launch and monitor Strategist/Teacher/Student runs with step tracking and telemetry. | Run timeline view | ✅ `POST /v1/runs`, `GET /v1/runs`, `GET /v1/runs/:id`, `POST /v1/runs/:id/progress`, `POST /v1/runs/:id/complete`, `POST /v1/runs/:id/cancel`, `DELETE /v1/runs/:id` (7 endpoints) | ✅ `guideai run create`, `guideai run get`, `guideai run list`, `guideai run complete`, `guideai run cancel` (5 commands) | ✅ `runs.create`, `runs.get`, `runs.list`, `runs.updateProgress`, `runs.complete`, `runs.cancel` (6 MCP tools in `mcp/tools/runs.*.json`) | Runs SDK (`guideai.runs`) | ✅ **Full Cross-Surface Parity Complete**: Contract (`guideai/run_contracts.py`), SQLite runtime (`guideai/run_service.py` ~535 lines), REST/CLI/MCP adapters (`guideai/adapters.py`), CLI commands (5), REST endpoints (7), MCP tool manifests (6), parity test suite (`tests/test_run_parity.py` - 22 passing tests in 0.22s covering create/get/list/progress/complete/cancel/errors/step-tracking across all three surfaces), unified execution record validation + telemetry (`run.created`, `run.progress`, `run.completed`), `BUILD_TIMELINE.md` #74-75 |
| Compliance checklist | Enforce agent compliance workflow and evidence capture. | Compliance dashboard | `POST /v1/compliance/checklists`, `POST /v1/compliance/steps`, `GET /v1/compliance/checklists`, `GET /v1/compliance/checklists/:id`, `POST /v1/compliance/checklists/:id/validate` | `guideai compliance create-checklist`, `guideai compliance record-step`, `guideai compliance list`, `guideai compliance get <id>`, `guideai compliance validate <id>` (5 commands) | ✅ 5 MCP tool manifests: `compliance.createChecklist`, `compliance.recordStep`, `compliance.listChecklists`, `compliance.getChecklist`, `compliance.validateChecklist` (`mcp/tools/compliance.*.json`) | Compliance SDK (`guideai.compliance`) | ✅ **Full CLI/REST/MCP Parity Complete (Milestone 1)**: Contract (`COMPLIANCE_SERVICE_CONTRACT.md`), in-memory service (`guideai/compliance_service.py` ~375 lines), REST/CLI/MCP adapters (`guideai/adapters.py`), CLI commands (5), MCP tool manifests (5), parity test suite (`tests/test_compliance_service_parity.py` - 17 passing tests), WORM audit log linkage, `docs/COMPLIANCE_CONTROL_MATRIX.md` |
| Action capture & replay | Record build actions and replay reproducible steps. | Action timeline | `POST /v1/actions`, `POST /v1/actions:replay` | `guideai record-action`, `guideai list-actions`, `guideai get-action`, `guideai replay-actions`, `guideai replay-status` (5 commands) | ✅ 5 MCP tool manifests: `actions.create`, `actions.list`, `actions.get`, `actions.replay`, `actions.replayStatus` (`mcp/tools/actions.*.json`) | Action SDK (`guideai.actions`) | ✅ **Full CLI/REST/MCP Parity Complete**: Contract (`ACTION_SERVICE_CONTRACT.md`), service (`guideai/action_service.py`), REST/CLI/MCP adapters (`guideai/adapters.py`), CLI commands (5), MCP tool manifests (5), CLI parity tests (`tests/test_cli_actions.py`), checksum verification, runbook (`docs/README.md`), telemetry (`tests/test_telemetry_integration.py`) |
| VS Code extension parity | Provide IDE sidebar, plan composer, and execution tracker that mirror platform/CLI flows. | ✅ **Milestone 1**: Behavior Sidebar (list/search/detail/insert), Workflow Explorer (list/run), Plan Composer webview; ⏳ **Milestone 2**: Compliance Review panel, Execution Tracker, Analytics Dashboard | ⏳ REST endpoints planned Milestone 2 | ✅ Via CLI subprocess: `behaviors list/search/get`, `workflow list-templates/run/status`, `telemetry emit`; ⏳ `compliance`, `actions`, `analytics` commands not yet integrated | ⏳ MCP integration planned | IDE SDK (`guideai.ide`) | ✅ **Milestone 1 Complete**: 11 TypeScript files (~1,100 lines), 7 commands, 2 tree views, 2 webview panels, GuideAIClient with JSON subprocess + telemetry, webpack build (51.1 KiB, 0 vulnerabilities), runtime validated (`extension/`, `BUILD_TIMELINE.md` #41-42); ⏳ **Gaps**: No compliance/actions/analytics UI, no MCP tool consumption yet |
| Telemetry infrastructure | Emit and query telemetry events for metrics pipeline and observability. | ⏳ Dashboard telemetry widgets (`web-console/dashboard/src/telemetry.ts`, `web-console/dashboard/src/hooks/*.ts`) | ⏳ `POST /v1/telemetry/events`, `GET /v1/telemetry/sessions` planned | ✅ `guideai telemetry emit`, `guideai telemetry query`, `guideai telemetry dashboard` | ✅ `telemetry.emit`, `telemetry.query`, `telemetry.dashboard` | Telemetry SDK (`guideai.telemetry`) | ✅ **E4 Telemetry Complete**: FileTelemetrySink + CLI commands (emit/query/dashboard), VS Code instrumentation, Python service integration, MCP tools (telemetry.query, telemetry.dashboard), 8 E4 domain events + 8 quality gate/flag events, parity test suite (`tests/test_telemetry_parity.py` - 15 passing tests); ⏳ REST query/retrieval APIs pending |
| Metrics & analytics | Track adoption and efficiency KPIs aligned with PRD success metrics (behavior reuse %, token savings %, completion rate, compliance coverage %). | ✅ **Metabase dashboards operational** (http://localhost:3000) with 4 PRD metric dashboards (2025-10-20); ⏳ VS Code analytics panel pending | ✅ `GET /v1/analytics/kpi-summary`, `GET /v1/analytics/behavior-usage`, `GET /v1/analytics/token-savings`, `GET /v1/analytics/compliance-coverage` (2025-10-20) | ✅ `guideai analytics project-kpi` (2025-10-16) | ✅ 4 MCP tool manifests: `analytics.kpiSummary`, `analytics.behaviorUsage`, `analytics.tokenSavings`, `analytics.complianceCoverage` (`mcp/tools/analytics.*.json`) (2025-10-20) | Analytics SDK (`guideai.analytics`) | ✅ **Production-Ready (Milestone 1)**: DuckDB warehouse (`data/telemetry.duckdb` with 8 tables/views), warehouse client (`guideai/analytics/warehouse.py`), schema DDL (`docs/analytics/prd_metrics_schema.sql`), migration script (`scripts/init_duckdb_schema.py`), REST API endpoints (4), MCP tool manifests (4), parity test suite (`tests/test_analytics_parity.py` - 10 passing tests), **Metabase visualization** (`docker-compose.analytics-dashboard.yml` - Podman-based deployment, 4 dashboard definitions in `docs/analytics/dashboard-exports/`, setup guide `docs/analytics/metabase_setup.md`), KPI projector (`guideai/analytics/telemetry_kpi_projector.py` with unit tests), `BUILD_TIMELINE.md` #61-62; ⏳ Flink production deployment, VS Code panel integration pending |
| MetricsService real-time aggregation | Provide real-time metrics caching and streaming for dashboard updates, distinct from batch Analytics queries. | ⏳ Dashboard SSE integration pending | ✅ `GET /v1/metrics/summary`, `POST /v1/metrics/export`, `POST /v1/metrics/subscriptions`, `DELETE /v1/metrics/subscriptions/{subscription_id}` (4 endpoints) | ✅ `guideai metrics summary` (with --format table/json, PRD target comparison), `guideai metrics export` (with --format json/csv/parquet) (2 commands) | ✅ `metrics.getSummary`, `metrics.export`, `metrics.subscribe` (3 MCP tools) | Metrics SDK (`guideai.metrics_service`) | ✅ **COMPLETE (2025-10-22)**: Contracts (`guideai/metrics_contracts.py` ~110 lines), service (`guideai/metrics_service.py` ~450 lines - SQLite cache with 30s TTL, AnalyticsWarehouse integration, SSE streaming), adapters (`guideai/adapters.py` +~230 lines), CLI commands (~150 lines in `guideai/cli.py` with table rendering + PRD target comparison ✓/✗ indicators), REST endpoints (~70 lines in `guideai/api.py` with _ServiceContainer integration), MCP manifests (~240 lines total in `mcp/tools/metrics.*.json` with draft-07 schemas), parity test suite (`tests/test_metrics_parity.py` - 19/19 passing tests in 2.99s across 5 classes: GetSummaryParity, ExportMetricsParity, SubscriptionParity, CacheBehavior, AdapterConsistency), live validation (CLI table output with warehouse data showing behavior_reuse_pct 100%, average_token_savings_pct 0.33%, REST curl tests successful), bug fix (None handling in metrics_service.py), warehouse integration operational (DuckDB queries returning actual seeded data), `BUILD_TIMELINE.md` #77 |
| Agent authentication & authorization | Centralize OAuth/OIDC flows, JIT consent, MFA, and policy enforcement for agents. | ⏳ Agent auth console planned Milestone 2 | ✅ `POST /v1/auth/grants`, `GET /v1/auth/grants`, `POST /v1/auth/policy-preview`, `DELETE /v1/auth/grants/{grant_id}` (4 endpoints - 2025-10-23) | ✅ `guideai auth ensure-grant`, `guideai auth list-grants`, `guideai auth policy-preview`, `guideai auth revoke`, `guideai auth login`, `guideai auth status`, `guideai auth refresh`, `guideai auth logout` (8 commands - device flow 2025-10-23) | ✅ MCP tool manifests: `auth.ensureGrant`, `auth.revoke`, `auth.listGrants`, `auth.policy.preview`, `auth.deviceLogin`, `auth.authStatus`, `auth.refreshToken`, `auth.logout` (`mcp/tools/auth.*.json` - 8 tools) | Auth SDK (`guideai.auth`) | ✅ **Phase 1 Service Parity COMPLETE (2025-10-23)**: Proto definitions (`proto/agentauth/v1/agent_auth.proto`), JSON schemas (`schema/agentauth/v1/agent_auth.json`), scope catalog (`schema/agentauth/scope_catalog.yaml`), policy bundle (`policy/agentauth/bundle.yaml`), MCP tool manifests (4 authorization + 4 device flow = 8 total), SDK implementation (`guideai/agent_auth.py` + `guideai/device_flow.py`), **REST API operational** (4 auth endpoints in `guideai/api.py` lines 668-735 with FastAPI TestClient validation), **CLI commands operational** (8 commands: 4 authorization + 4 device flow with 28/28 device flow tests passing), **adapters complete** (`CLIAgentAuthServiceAdapter`, `RestAgentAuthServiceAdapter`, `MCPAgentAuthServiceAdapter` in `guideai/adapters.py` lines 1782-2087), **comprehensive parity test suite** (`tests/test_agent_auth_parity.py` - **17/17 tests passing** in 0.13s across 5 test classes: TestEnsureGrantParity, TestListGrantsParity, TestPolicyPreviewParity, TestRevokeGrantParity, TestAdapterConsistency), contract tests (`tests/test_agent_auth_contracts.py`), architecture doc (`docs/AGENT_AUTH_ARCHITECTURE.md` §§12,16-19), MFA enforcement policy, telemetry integration (`tests/test_telemetry_integration.py`), **MCP Device Flow COMPLETE** (`guideai/mcp_server.py` - production stdio JSON-RPC server with 59 tools discovered, `guideai/mcp_device_flow.py` - 600 lines service layer, `tests/test_mcp_device_flow.py` - 12/27 passing with core logic validated, `examples/test_mcp_server.py` - 4/4 validation tests passing, `docs/DEVICE_FLOW_GUIDE.md` - 400+ line MCP section with Claude Desktop config), **Token Storage Parity**: CLI/MCP share KeychainTokenStore (macOS keychain/Linux secretstorage/Windows Credential Manager) or FileTokenStore fallback with consistent AuthTokenBundle, authenticate via CLI → tokens available to MCP and vice versa, `BUILD_TIMELINE.md` #77-78-79-83, `PRD_ALIGNMENT_LOG.md` 2025-10-23; ⏳ **Production Auth Pending**: token vault hardening, policy engine enforcement, JIT consent UI across surfaces |
| Consent UX & telemetry | Deliver consistent consent experiences with instrumentation across surfaces. | Dashboard modal + telemetry widgets | `/v1/auth/consent` (planned) | `guideai auth consent` device flow | `auth.ensureGrant` (CONSENT_REQUIRED flow) | Auth SDK (`guideai.auth`), VS Code extension | `docs/CONSENT_UX_PROTOTYPE.md`, `designs/consent/mockups.md`, `docs/AGENT_AUTH_ARCHITECTURE.md` §§18-19, `web-console/dashboard/src/telemetry.ts`, `web-console/dashboard/src/hooks/useConsentTelemetry.ts`, telemetry events snapshot, CMD-007 complete |
| Agent review automation | Schedule cross-functional agent feedback loops for artifacts (Engineering, DX, Compliance, Product). | Review scheduler modal (planned) | `/v1/reviews/*` | `guideai agents review` | `reviews.run`, `reviews.list`, `reviews.get` | Review SDK (`guideai.reviews`) | `PRD_AGENT_REVIEWS.md` (2025-10-15), planned action logs CMD-005, parity tests TBD |
| Runtime agent orchestration | Dedicated agent selection and switching for Strategist → Teacher → Student runs with policy-driven heuristics. | ⏳ Execution Tracker badges + override UI (planned) | ⏳ `POST /v1/agents:assign`, `POST /v1/agents:switch`, `GET /v1/agents/status` (contract drafted) | ⏳ `guideai agents assign`, `guideai agents switch`, `guideai agents status` (planned) | ⏳ `agents.assign`, `agents.switch`, `agents.status` (planned MCP tools) | ⏳ Agent Orchestrator SDK (`guideai.agent_orchestrator`) planned | `AGENT_ORCHESTRATOR_SERVICE_CONTRACT.md`, `PRD.md` Future Enhancements – Runtime Agent Orchestration, `PRD_NEXT_STEPS.md` Phase 3 runtime orchestration workstream |
| Task assignment orchestration | Surface remaining milestone tasks mapped to functions/agents for parity planning. | Planning dashboard widget (planned) | `POST /v1/tasks:listAssignments` | `guideai tasks --function <owner>` | `tasks.listAssignments` | Task SDK stub (`guideai.task_assignments`) | `guideai/task_assignments.py`, `tests/test_task_assignments.py`, `PRD_NEXT_STEPS.md` function mapping, MCP schema `mcp/tools/tasks.listAssignments.json` |
| Workflow Engine (BCI) | Role-specific workflow templates with behavior-conditioned inference, step orchestration, token tracking. | Template builder (planned) | `POST /v1/workflows/templates`, `GET /v1/workflows/templates`, `GET /v1/workflows/templates/:id`, `POST /v1/workflows/runs`, `GET /v1/workflows/runs/:id`, `PATCH /v1/workflows/runs/:id/status` | `guideai workflow create-template`, `guideai workflow list-templates`, `guideai workflow get-template`, `guideai workflow run`, `guideai workflow status` (5 commands) | ✅ MCP tool manifests (5): `workflow.template.create`, `workflow.template.list`, `workflow.template.get`, `workflow.run.start`, `workflow.run.status` (`mcp/tools/workflow.*.json`) | Workflow SDK (`guideai.workflow_service`) | ✅ **Full Cross-Surface Parity Complete (Milestone 1)**: Contract (`WORKFLOW_SERVICE_CONTRACT.md`), SQLite runtime (`guideai/workflow_service.py` ~600 lines), REST/CLI/MCP adapters (`guideai/adapters.py`), CLI commands (5), MCP tool manifests (5), service integration tests (`tests/test_workflow_service.py` - 18 passing tests), parity test suite (`tests/test_workflow_parity.py` - 17 passing tests), BCI algorithm validation, example workflow (`examples/strategist_workflow_steps.json`), telemetry events (`workflow.*`, `plan_created`, `execution_update`), `BUILD_TIMELINE.md` #40 |
| BCI retrieval & prompting contracts | Schemas for behavior retrieval, prompt composition, citation validation, and trace analysis underpinning Milestone 2 BCI rollout. | ⏳ Plan Composer preview pending | ✅ SDK complete: dataclasses (`guideai/bci_contracts.py`), service (`guideai/bci_service.py`), adapters (REST/MCP in `guideai/adapters.py`), FastAPI endpoints (`guideai/api.py` `/v1/bci:*`); ⏳ Production deployment pending | ✅ `guideai bci rebuild-index` (Phase 1); ⏳ Additional subcommands pending | ✅ 11 MCP tool manifests: `bci.retrieve`, `bci.retrieveHybrid`, `bci.rebuildIndex`, `bci.composePrompt`, `bci.composeBatchPrompts`, `bci.parseCitations`, `bci.validateCitations`, `bci.computeTokenSavings`, `bci.segmentTrace`, `bci.detectPatterns`, `bci.scoreReusability` (`mcp/tools/bci.*.json`) | ✅ BCI SDK exports (`guideai.bci_contracts`, `BCIService`) in `guideai/__init__.py` | ✅ **SDK + MCP + CLI Phase 1 Complete**: Schemas (`schema/bci/v1/*.json`), SDK dataclasses with ForwardRef serialization fix, service logic leveraging BehaviorService + BehaviorRetriever, REST/MCP adapters, 11 MCP tool manifests (updated `bci.rebuildIndex` schema), FastAPI integration, parity test suite (`tests/test_bci_parity.py` - 7 passing tests covering dataclass round-trips, service logic, adapter parity, REST endpoints, retriever delegation), package exports, CLI `bci rebuild-index` command (`guideai/cli.py` `_command_bci_rebuild_index`), semantic dependency bundling (`pyproject.toml` [semantic] extra); `BUILD_TIMELINE.md` #60 & #67, `PRD_ALIGNMENT_LOG.md` "BCI SDK Parity Validation (2025-10-17)" & "BehaviorRetriever P0 Production Readiness (2025-10-22)"; ⏳ Additional CLI subcommands and production deployment pending |
| SDK distribution & versioning | Publish language-specific SDKs with consistent contracts, semantic versioning, and release automation. | N/A (surfaced via docs) | OpenAPI / REST schemas | `guideai` (Python package), future release tooling | MCP schemas for generated clients | `docs/SDK_SCOPE.md`, PyPI/npm release plan, ActionService/AgentAuth contract references | Release checklist TBD, pipeline design forthcoming |
| CI/CD pipelines & deployment automation | Standardize checks (secret scan, tests, builds) and deployment telemetry across hosts. | Web deploy console (planned) | `/v1/deployments/*` (future) | ✅ `guideai scan-secrets` | ✅ MCP tool manifest: `security.scanSecrets` (`mcp/tools/security.scanSecrets.json`) | `.github/workflows/ci.yml`, `docs/AGENT_DEVOPS.md`, `docs/GIT_STRATEGY.md`, `scripts/install_hooks.sh` | ✅ Secret scanning complete: CLI command (`guideai/cli.py`), pre-commit hook (`.pre-commit-config.yaml`), helper scripts (`scripts/scan_secrets.sh`, `scripts/install_hooks.sh`), CI integration (`.github/workflows/ci.yml`), MCP tool manifest, regression tests (`tests/test_scan_secrets_cli.py`), governance docs (`SECRETS_MANAGEMENT_PLAN.md`, `AGENTS.md`); ⏳ Deploy automation pending |

> **Note:** Add additional rows as new capabilities are introduced. Keep descriptions concise but auditable, and reference concrete endpoints/commands.

### Upcoming capability checkpoints
- ⏳ **Financial governance telemetry** – Planned milestone for surfacing budget/ROI dashboards across Web/API/CLI/MCP once Finance Agent workflows launch (`behavior_validate_financial_impact`).
- ⏳ **GTM launch readiness** – Placeholder row for campaign orchestration tooling and adoption telemetry owned by the Go-to-Market Agent (`behavior_plan_go_to_market`).
- ⏳ **Security posture reporting** – Future capability row to capture threat modeling status, auth scope coverage, and incident readiness metrics (`behavior_lock_down_security_surface`).
- ⏳ **Accessibility compliance audits** – Planned parity checkpoints for WCAG validation tooling and reporting across surfaces (`behavior_validate_accessibility`).
- ⏳ **Data science experiment governance** – Placeholder for cross-surface experiment review tooling, drift alerts, and telemetry owned by the Data Science Agent (`behavior_instrument_metrics_pipeline`, `behavior_align_storage_layers`).
- ⏳ **AI research pipeline** – Planned checkpoints for research harness distribution, safety evaluation reporting, and behavior harvesting workflows managed by the AI Research Agent (`behavior_curate_behavior_handbook`, `behavior_lock_down_security_surface`).

### E4 Capabilities (Epic GUIDEAI-278 — Learning Loop, Analytics, and Governance) ✅ SHIPPED
| Capability | Description | CLI | MCP Tool | Parity Evidence |
| --- | --- | --- | --- | --- |
| Feature Flags | Boolean, percentage, and user-list flag evaluation | `guideai flags list\|get\|set` | `flags.list`, `flags.get`, `flags.set` | `tests/test_feature_flags.py` (46 tests) |
| Auto-Reflection | Post-run reflection pipeline gated by feature flag | Triggered via execution loop | N/A (internal) | `tests/test_auto_reflection.py` (8 tests) |
| Review Queue | Human-in-the-loop candidate review | N/A | MCP review tools | `tests/test_reflection_review_queue.py` (24 tests) |
| Lifecycle Policies | Behavior deprecation, promotion, decay | N/A | N/A (internal) | `tests/test_behavior_lifecycle_policies.py` (18 tests) |
| Benchmark Corpus | Domain-specific regression test tasks | N/A | N/A (internal) | `packages/midnighter/tests/test_domain_benchmark.py` (28 tests) |
| Comparison Harness | A/B BCI vs baseline evaluation | N/A | N/A (internal) | `packages/midnighter/tests/test_comparison_harness.py` (25 tests) |
| Quality Gates | CI-enforced accuracy and token thresholds | N/A | N/A (internal) | `tests/test_quality_gates.py` (31 tests) |
| Pack Bootstrap/Rollback | Backward-compat migration for existing workspaces | `guideai pack bootstrap\|rollback\|status` | `pack.bootstrap`, `pack.rollback`, `pack.status` | `tests/test_backward_compat_migration.py` (29 tests) |

## Release checklist hook
- Every release PR must include a checkbox referencing the relevant capability matrix rows (e.g., "☑ Capability matrix updated for `<feature>`").
- Reviewers must confirm:
  - The matrix row lists all surfaces with implemented status.
  - Linked parity evidence (contract tests, audit log IDs, action records) is accessible.
  - Telemetry fields required by `TELEMETRY_SCHEMA.md` are documented.

## Change log template
When updating this file, copy the snippet below into the PR description and action log:
```
Capability Matrix Update
- Capability: <name>
- Surfaces touched: <Web/API/CLI/MCP/SDK>
- Evidence: <tests executed, audit log IDs, action IDs>
- guideai record-action --artifact docs/capability_matrix.md --summary "Update capability matrix for <feature>" --behaviors <ids>
```

## Governance reminders
- Changes to this matrix must be accompanied by an action record (`guideai record-action`) and logged in `PRD_ALIGNMENT_LOG.md`.
- If the update introduces a new capability, add follow-up tasks to `PRD_NEXT_STEPS.md` and `PROGRESS_TRACKER.md` as needed.
- Verify that capability telemetry is covered by the metrics pipeline targets (70% behavior reuse, 30% token savings, 80% completion, 95% compliance coverage).

_Last updated: 2026-03-19 (Epic E4 capabilities shipped)_
